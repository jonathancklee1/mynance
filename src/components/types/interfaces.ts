export interface recurringExpenseItem {
    id: string;
    name: string;
    amount: number;
}

export interface expenseItem {
    id: string;
    date: string;
    expense: string;
    cost: string;
}
